profile_schema_version: 1

name: School Setup
id: school
version: 1
description: School dev setup

tags:
  - development
  - school

supported_os:
  - windows

packages:
  all:
    - flutter
    - vscode

  windows:
    - win_terminal
