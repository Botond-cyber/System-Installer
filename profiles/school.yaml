profile_schema_version: 1

name: School Setup
id: school
version: 1
description: School dev setup

tags:
  - development
  - school

supported_os:
  - windows

packages:
  all:
    - git
    - vscode

  windows:
    - windows-tools
