name: Flutter

depends:
  modules:
    linux:
      - git
      - unzip
    windows:
      - git

actions:
  install:
    linux:
      fedora:
        - echo "Flutter install step placeholder"
    windows:
      - winget install Google.Flutter
