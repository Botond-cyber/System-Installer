name: Google Chrome

actions:
  install:
    linux:
      fedora:
        - sudo dnf install fedora-workstation-repositories
        - sudo dnf config-manager setopt google-chrome.enabled=1
        - sudo dnf install google-chrome-stable

    windows:
      - winget install -e --id Google.Chrome --scope user
