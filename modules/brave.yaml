name: Brave Browser

depends:
  scripts:
    linux:
      - update

actions:
  install:
    linux:
      fedora:
        - curl -fsS https://dl.brave.com/install.sh | sh
    windows:
      - winget install -e --id Brave.Brave --scope user
