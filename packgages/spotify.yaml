profile_schema_version: 0

name: Spotify
id: spotify
version: 1.0
author: Botond
description: Spotify music streaming client
type: app
tags:
  - media
  - music

supported_os:
  - fedora
  - ubuntu
  - windows

dependencies:
  fedora:
    - flatpak
    - flathub
  ubuntu:
    - flatpak
    - flathub
  windows:
    - winget

check:
  fedora: |
    flatpak --user info com.spotify.Client &> /dev/null
  ubuntu: flatpak --user info com.spotify.Client &> /dev/null
  windows: |
    where.exe spotify.exe >nul

install:
  fedora: |
    # Install (if not already)
    sudo flatpak install -y --noninteractive flathub com.spotify.Client
  ubuntu: |
    flatpak install -y --noninteractive flathub com.spotify.Client
  windows: |
    # Install via WinGet (non-interactive, skip upgrade if exists)
    winget install --id Spotify.Spotify --silent --accept-package-agreements --no-upgrade

configure:
  fedora:

  ubuntu:

  windows:
