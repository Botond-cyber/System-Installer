package_schema_version: 1

name: Example Package
id: example_package
version: 1
author: John Doe
description: An example package
type: app
tags:
  - example

supported_os:
  - fedora
  - ubuntu
  - windows

dependencies:
  fedora:
    - example_dependency1
    - example_dependency2
  ubuntu:
    - example_dependency1
    - example_dependency2
  windows:
    - example_dependency3

check:
  fedora:
    - example -v
  ubuntu:
    - example --version
  windows:
    - idk how to check on windows

install:
  fedora:
    - sudo dnf install example
  ubuntu:
    - sudo apt install example
  windows:
    - winget install --id Example.Example --silent --accept-package-agreements --no-upgrade

configure:
  fedora:
    - example config
  ubuntu:
    - example config
  windows:
    - example config
